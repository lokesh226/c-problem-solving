// count sub arrays having sum of elements at even and odd positions equal
// find the peak element in a array 
// find sub array of length k with maximum peak { given an array a of length n and another variable k. we have to find a sub array 
// of length k which has maximum peak inside it}
// replace  every element of a array with previous element 
// rearrange array such that even index elements are smaller  and odd elements are greater 

